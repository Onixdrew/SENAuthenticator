// Importa el paquete necesario de Flutter.
import 'package:flutter/material.dart';

// Define los colores constantes utilizados en la aplicación.
const primaryColor = Color.fromRGBO(203, 73, 101, 1);
const secondaryColor = Color.fromRGBO(40, 40, 40, 1);
